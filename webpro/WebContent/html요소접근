html 요소 검색하기

1. id 이름을 이용하여 검색하기
	var a = document.getElementById('id이름')
	var a = document.querySelector('#id이름')
	
2. class 이름을 이용하여 검색하기
	var b = document.getElementsByClassName('class이름') // 리스트
	b[0], b[1], ...
	
	var b = document.querySelector('.class이름')		   // 결과는 한 개
	var b = document.querySelectorAll('.class이름')	   // 결과는 리스트
	b[0], b[1], ...	   
	
	
3. name을 이용하여 검색하기
	var c = document.getElementsByName('class이름')	// 결과는 리스트 c[0], c[1], ...
	
4. tag 이름을 이용하여 검색
 	var d = document.getElementsByTagName('tag이름')	// 리스트 d[0], d[1], ...
 	var d = document.querySelector('tag이름')			// 결과는 한 개
 	var d = document.querySelectorAll('tag이름')		// 리스트 d[0], d[1], ...
 	
값 가져오기
innerHTML	- h1, div, span, p, td, th			// HTML이 포함되어있으면 innerHTML
innerText/TextContent - h1, div, span, p, td	// HTML이 포함되지않으면 innerText

value 	- input 요소, textarea, select에서 값 가져오기
